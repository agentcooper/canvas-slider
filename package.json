{
  "name": "slider-challange",
  "version": "1.0.0",
  "description": "Canvas slider with lazy loading",
  "main": "index.js",
  "scripts": {
    "build": "browserify src/slider.js --standalone Slider -t babelify > dist/slider.build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Artem Tyurin",
  "license": "ISC",
  "devDependencies": {
    "babelify": "^6.1.2"
  }
}
